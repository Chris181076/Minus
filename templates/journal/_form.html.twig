{{ form_start(form) }}
    {{ form_row(form.date) }}
    {{ form_row(form.meal) }}
    {{ form_row(form.nap) }}
    {{ form_row(form.diaper_time) }}
    {{ form_row(form.diaper_type) }}
    {{ form_row(form.activity) }}
    {{ form_row(form.note) }}
    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
