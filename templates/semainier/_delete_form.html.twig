<form method="post" action="{{ path('app_semainier_delete', {'id': semainier.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ semainier.id) }}">
    <button class="btn">Delete</button>
</form>
